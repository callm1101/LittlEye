# 小眼睛（LittlEye）app：桌面学习伴侣（Graduate Desktop Companion）— 实施规格

## 目标

构建一个面向硕博研究生的轻量跨平台桌面应用，常驻桌面而不打扰工作：按用户配置的时间提醒起身、喝水；提供可拖动、可置顶、可直接书写的临时便利贴；在 macOS、Windows 与 Linux 可运行。

## 推荐技术栈

- **桌面框架：Tauri 2 + Rust**：安装包和内存占用通常明显小于 Electron，原生支持 Windows/macOS/Linux。
- **前端：React + TypeScript + Vite**：适合构建紧凑、可维护的小组件界面。
- **样式：Tailwind CSS**（或 CSS Modules）：优先无重型组件库的简洁 UI。
- **本地数据：SQLite（tauri-plugin-sql）**：只保存设置、提醒记录和可选的浏览器计时状态；便利贴正文只保存在当前进程内存中，不写入磁盘，不收集或上传用户数据。
- **通知：tauri-plugin-notification**，并配合应用内弹窗：系统通知用于应用在后台时；小人弹窗确保提醒可见。
- **动画：CSS/Framer Motion + 本地 SVG/Lottie 资源**：小人做 1–2 秒的轻量循环动作，避免远程资源依赖。

## 核心体验与范围

### 1. 定时起身喝水提醒

- 默认每 **60 分钟**触发一次；首次启动时从用户完成引导或应用启动起计时。
- 设置页可将提醒间隔调整为 30 / 45 / 60 / 90 / 120 分钟，将“稍后提醒”时长调整为 5 / 10 / 15 / 20 分钟，并支持暂停和恢复。
- 到点显示一个小而友好的无边框弹窗：动画小人 + 文案“该起来活动并喝口水啦” + 三个操作：
  - **完成**：记录一次完成，关闭弹窗，开始下一周期。
  - **稍后 N 分钟**：关闭并仅按设置中的“稍后提醒”时长延迟本次提醒，默认 10 分钟。
  - **跳过**：记录跳过，开始下一周期。
- 同时发送系统通知（标题“活动一下，喝点水”）。通知权限被拒绝时，应用内弹窗仍必须正常工作。
- 设置页提供“喝水语音提示”开关；启用后，每次喝水提醒弹出时使用系统中文语音播报一次。语音能力不可用时静默降级，不得影响弹窗、通知或后续调度。
- 不在全屏演示、屏幕共享或系统勿扰状态下强制抢焦点；可通知但不激活窗口。提供“专注时暂停提醒”开关。
- 仅在应用运行时保证提醒；可选增强项是在开机启动后常驻系统托盘。不要承诺应用被彻底退出后仍能提醒。

### 2. 桌面置顶便利贴

- 默认显示一个无边框的便利贴窗口（建议约 340 × 420 px），主体是可直接输入纯文本的编辑区；顶部显示角色、下次提醒倒计时、置顶开关和设置入口。
- 顶部标题区域必须支持鼠标拖动窗口；文本编辑区、按钮和其他交互元素不得触发窗口拖动。窗口尺寸固定，边缘不得触发缩放光标或调整尺寸操作，并在不同 DPI 缩放下保持可用。
- 支持“始终置顶”，用户可随时关闭；记住窗口位置、置顶状态与可见性。
- 便利贴背景支持 **10%–100%** 透明度调节，默认 **75%**，并在本机保存用户选择；仅降低背景透明度，文字与交互控件须保持清晰可用。
- 支持最小化到系统托盘；托盘菜单提供：显示/隐藏组件、立即提醒、暂停/恢复、打开设置、退出。
- “置于桌面上”在不同系统含义不一致：实现为普通无边框窗口的 **always-on-top**，而非嵌入桌面壁纸层。文案明确使用“始终置顶”。
- 不应阻塞鼠标或键盘；可选“锁定位置”防止误拖动。

### 3. 会话级便利贴内容

- 用户可直接在便利贴主体中书写和修改纯文本，不需要新建笔记、填写标题或进入独立编辑页面。
- 便利贴正文只存在于当前应用进程的内存状态中，禁止写入 SQLite、LocalStorage、配置文件、日志、崩溃报告或任何远程服务。
- 隐藏窗口或最小化到托盘不视为退出，当前运行期间便利贴内容应继续保留；用户通过托盘“退出”或应用进程结束后，正文必须随进程消失，下次启动显示空白便利贴。
- 提供明确的“清空”操作；空内容时禁用该操作。第一版不提供便利贴历史、恢复、搜索、固定单条内容、富文本、Markdown 渲染、附件、同步或 AI 功能。

### 4. 网站浏览时长提醒（可选启用）

- 用户可在桌面端维护最多 32 个网站域名组成的白名单，默认包含 `bilibili.com` 与 `xiaohongshu.com`。输入完整网页地址时只提取域名，不保存路径、查询参数或完整 URL。
- 用户**主动启用并在扩展选项中逐域名授权**后，扩展仅在白名单页面处于前台、可见且聚焦时计时；默认阈值为 **30 分钟**。
- 每个白名单域名单独累计浏览时长，任一域名达到阈值时显示独立应用内弹窗，注明触发提醒的域名。操作为：
  - **我回来了**：重置本轮计时。
  - **再看 10 分钟**：本轮只延迟 10 分钟。
  - **今天不再提醒**：暂停至次日当地时间 00:00。
- 只在目标标签页为当前活动标签、浏览器窗口位于前台、页面可见时计时；切换标签、最小化浏览器、锁屏、系统睡眠或页面失焦时暂停计时。
- 不读取标题、账户信息、搜索词、页面正文、路径、查询参数或浏览历史；仅传递最小事件（匹配的白名单域名、活跃/非活跃状态、时间戳）。不上传浏览记录。
- 设置页提供启用开关、30/45/60 分钟阈值、网站白名单、各域名今日累计、今日暂停状态与“清除本地浏览计时”操作。浏览监测配置与配对令牌加载完成后开关才可操作；隐私说明常驻显示在开关下方，点击开关应立即启停并保存，不依赖系统原生确认框，也不得被异步初始化结果回写覆盖。
- 白名单新增域名必须在扩展选项中由用户手动确认网站权限；扩展使用可选主机权限并动态注册内容脚本，不得默认获得所有网站的访问权。移除域名后，在扩展下次同步白名单时撤销对应权限。
- 扩展最低支持 Chromium 102，以使用 Manifest V3 的 `optional_host_permissions`；较旧浏览器应显示明确的不兼容提示而不是静默失效。
- 因浏览器权限、扩展未安装或本地通信不可用而无法监测时，显示明确但非阻塞的状态提示；不影响起身喝水和便利贴功能。

## 信息架构与界面

1. **首次引导**：说明提醒机制 → 选择提醒间隔与稍后时长 → 请求通知权限 → 启动桌面便利贴。
2. **桌面便利贴**：可拖动标题区、角色/状态、倒计时、置顶开关、设置按钮、会话级纯文本编辑区、暂停/恢复与清空操作。
3. **提醒弹窗**：动画角色、短提示、完成/稍后/跳过动作；键盘可操作且焦点清晰。
4. **设置页**：提醒间隔、稍后时长、喝水语音提示、提醒启用、专注暂停、通知开关、开机启动、窗口置顶、便利贴透明度、网站白名单及各域名累计、持久数据导出/清除；便利贴正文不参与导出。

视觉原则：温和、专注、低认知负担；暖色点缀、清晰对比度、支持系统浅色/深色模式；避免医学承诺或强制性措辞。

## 架构设计

### 前端

- `AppShell`：路由与主题。
- `WidgetWindow`：可拖动、可置顶的桌面便利贴；承载会话级文本编辑区和简要提醒状态。
- `ReminderWindow`：独立提醒弹窗。
- `SettingsPage`：配置。
- `useReminderScheduler`：状态机驱动的计时与恢复逻辑，避免多个计时器并存。
- `useBrowserUsageMonitor`：消费浏览器扩展传来的最小活跃事件，并按白名单域名分别计算累计时长。
- `repositories/`：对 SQLite 的数据访问抽象，不让 UI 直接拼 SQL；便利贴正文不得进入 repository 层。

### Tauri/Rust

- 管理主窗口、提醒窗口和托盘；通过命令暴露 `show_reminder`、`set_always_on_top`、`get_window_state` 等最小接口。
- 主窗口与系统托盘必须显式使用应用图标，Windows 任务栏和通知区域不得显示空白图标。
- 提供经本地认证的原生消息通道，供配套浏览器扩展发送目标站点活跃/失活事件；只接受白名单扩展和预定义消息格式。
- 应用启动时恢复配置与窗口状态，但不恢复便利贴正文；关闭主窗口时默认隐藏到托盘，不直接退出，通过托盘“退出”才结束进程并清除会话内容。
- 使用平台 API 检测勿扰/全屏能力时应优雅降级；不可用时不阻断核心提醒。

### 提醒状态机

`idle → scheduled → showing → completed | snoozed | skipped → scheduled`

- 所有状态变更写入提醒历史。
- 应用从休眠恢复、重启或系统时间变化后：重新计算下一次到期时间；若已过期，仅补发一次提醒，绝不连发积压提醒。
- 时间存储为 UTC 时间戳，界面以本地时区显示。

网站浏览提醒为每个白名单域名维护独立状态机：`disabled → monitoring → threshold_reached → alerted → reset | snoozed | muted_today`。以可信时间差累计，避免标签切换造成重复计时；应用或扩展重启后恢复未完成的当日累计值。

## 数据与隐私

SQLite 数据库位于应用专用数据目录；数据库迁移必须版本化。

建议表：

- `settings(key TEXT PRIMARY KEY, value TEXT, updated_at INTEGER)`
- `reminder_events(id TEXT PRIMARY KEY, scheduled_at INTEGER, action TEXT, acted_at INTEGER, metadata TEXT)`
- `browser_usage_sessions(id TEXT PRIMARY KEY, domain TEXT, started_at INTEGER, ended_at INTEGER NULL, active_seconds INTEGER)`
- `browser_usage_state(domain TEXT PRIMARY KEY, day_local TEXT, accumulated_seconds INTEGER, snoozed_until INTEGER NULL, muted_until INTEGER NULL, updated_at INTEGER)`

设置、提醒历史与可选浏览器计时默认仅在本地存储。设置页明确说明数据不离开设备，并提供 JSON 导出和“清除所有持久数据”二次确认。便利贴正文属于易失性会话数据，不进入数据库、备份或导出文件。

浏览器功能必须在首次启用前展示隐私说明：扩展仅在用户授权的白名单域名匹配时运行、收集字段、数据保留方式和撤销授权方法。将浏览器扩展作为独立、可审查的最小权限组件发布。

## 跨平台要求

- 使用 Tauri 的窗口、托盘、通知与文件系统 API，避免未封装的平台调用。
- **macOS**：处理通知授权、Dock/菜单栏行为与签名/公证发布流程；透明窗口启用 Tauri `macOSPrivateApi`，因此该构建不以 Mac App Store 上架为目标。
- **Windows**：验证通知、任务栏/托盘应用图标、DPI 缩放与安装包（MSI/NSIS）。
- **Linux**：验证 AppImage/Deb、常见桌面环境的托盘可用性；托盘不可用时提供窗口内替代入口。
- 使用逻辑像素和响应式布局；在 100%、125%、150% 缩放测试。
- 键盘导航、可见焦点、语义标签、足够色彩对比度为必需项。

## 项目结构

```text
graduate-desktop-companion/
├── src/
│   ├── components/        # 可复用 UI 与动画角色
│   ├── features/
│   │   ├── reminder/
│   │   └── settings/
│   ├── repositories/      # SQLite 数据访问
│   ├── hooks/
│   ├── lib/
│   └── styles/
├── src-tauri/
│   ├── src/               # 窗口、托盘、原生命令
│   ├── icons/
│   └── tauri.conf.json
├── browser-extension/     # Manifest V3 扩展；仅限白名单域名与本地消息
│   ├── manifest.json
│   ├── src/
│   └── README.md
├── tests/
├── docs/
└── README.md
```

## Codex 执行任务

1. 初始化 Tauri 2、React、TypeScript、Vite 项目，配置格式化、lint、类型检查和测试命令。
2. 建立 SQLite 迁移、repository 层和设置/提醒事件的数据模型；编写迁移与 repository 单元测试，并测试便利贴正文不会进入持久化层。
3. 实现提醒状态机及其单元测试：首次调度、完成、跳过、稍后、休眠恢复、系统时间前后跳变。
4. 实现 Manifest V3 浏览器扩展和桌面端本地消息通道：仅在用户授权的白名单域名动态运行，只上报匹配域名与网页的可见/活跃事件，提供安装、同步授权与卸载说明。
5. 实现按域名隔离的网站浏览时长状态机及测试：前台累计、失焦暂停、30 分钟阈值、10 分钟延迟、当日静默、重启恢复和重复弹窗防护。
6. 实现“生气但不羞辱用户”的角色提醒弹窗，并在设置中加入权限、阈值、状态和本地数据清除控制。
7. 实现桌面便利贴：顶部区域可拖动、正文可直接编辑、置顶状态与窗口位置持久化、托盘菜单和隐藏到托盘行为；正文仅保留在当前进程内存中。
8. 实现独立提醒弹窗、角色动画、系统通知及三种操作；处理通知权限拒绝与窗口重复打开。
9. 实现便利贴正文的会话生命周期、手动清空与退出清空测试，确保隐藏/显示期间保留，重启后为空。
10. 实现设置页、首次引导、浅深色主题、无障碍键盘操作与持久数据导出；便利贴正文不得导出。
11. 在 macOS、Windows、Linux CI 矩阵中运行格式化、类型检查、单元测试和构建；记录浏览器扩展兼容性与平台差异。
12. 完善 README：开发前置条件、启动/打包命令、扩展安装、隐私说明、已知限制与故障排查。

## 里程碑

| 阶段 | 交付物 |
| --- | --- |
| M1：骨架 | 可启动的 Tauri 应用、托盘、可拖动/置顶的便利贴、设置持久化 |
| M2：提醒 | 可配置的调度、提醒弹窗、通知、历史记录与测试 |
| M3：浏览器提醒 | 最小权限扩展、本地通信、可配置白名单网站累计浏览提醒 |
| M4：便利贴 | 会话级直接编辑、隐藏时保留、清空操作、退出后清除与窗口状态恢复 |
| M5：打磨 | 引导、动画、无障碍、主题、导出、跨平台构建 |

## 验收标准

- 在任意支持平台上，默认 60 分钟提醒可可靠触发；为测试可切换为 1 分钟或注入时钟。
- “完成”“稍后 N 分钟”“跳过”均只产生预期的一条后续调度与一条历史记录，其中 N 使用设置页配置的时长。
- 应用从休眠恢复或重启后不会丢失提醒设置和窗口状态；不会因过期任务连续弹窗。
- 用户安装并授权配套扩展后，任一白名单域名在活动、可见且聚焦状态累计 30 分钟会触发一次注明域名的提醒；不同域名分别累计，离开标签、锁屏、睡眠或最小化时不计时。
- 用户可新增、删除白名单域名；新增域名必须经浏览器权限确认后才开始监测，移除域名后对应内容脚本和主机权限被撤销。
- 浏览器扩展不会读取或上传完整 URL、标题、页面内容、账户信息或浏览历史；关闭功能或移除扩展后不再监测。
- 便利贴可显示/隐藏、通过顶部区域鼠标拖拽、切换置顶和最小化至托盘；窗口边缘不会触发尺寸调整，窗口位置与置顶状态跨重启保留。
- 便利贴透明度可在 10%–100% 间实时调节，首次使用默认为 75%，重启后恢复用户选择；macOS 上能实际透出窗口后的桌面内容。
- 启用喝水语音提示后，每次喝水提醒只播报一次；关闭或系统语音不可用时不播报且不影响其他提醒行为。
- Windows 主窗口任务栏和系统托盘均显示正确的应用图标。
- 用户可直接在便利贴正文区输入纯文本，编辑时不会误触发窗口拖动；隐藏到托盘再显示时内容仍在。
- 便利贴正文不会出现在 SQLite、LocalStorage、配置、日志或导出文件中；彻底退出并重新启动后正文为空，手动“清空”立即生效。
- 通知权限被拒绝、托盘不可用、平台特性不支持时，应用核心功能保持可用且不崩溃。
- 所有 lint、类型检查、单元测试通过；三个目标平台至少能完成构建，关键手动测试记录在 `docs/qa-checklist.md`。
