# LittlEye 项目协作指南

## 项目目标

小眼睛是使用 Tauri 2、Rust、React 与 TypeScript 构建的跨平台桌面学习伴侣。核心体验必须保持温和、低打扰，并默认将数据保留在本机。

## 代码边界

- `src/`：React 前端。组件只处理展示和交互；数据读写必须经 `repositories/`，不要在 UI 中直接访问 SQL 或存储实现。
- `src/hooks/`：计时、窗口状态等可复用逻辑。提醒必须只有一个调度器，避免重复计时器或重复弹窗。
- `src/features/`：按提醒、笔记、设置划分业务界面和状态。
- `src-tauri/`：Rust 原生能力。仅以最小、类型化 Tauri command 向前端暴露窗口、托盘、通知和本地通信能力。
- `browser-extension/`：独立的 Manifest V3 扩展。只可匹配用户明确授权的白名单域名，默认仅 `bilibili.com`；严禁读取页面内容、完整 URL、账户信息或浏览历史。

## 实现要求

- 使用 UTC 毫秒时间戳存储，展示时转换为本地时区。
- 所有数据库迁移必须可版本化、可重复执行；仓库层须有单元测试。
- 自动保存使用 500–800 ms 防抖，关闭编辑器前必须写完未落盘内容。
- 提醒动作（完成、稍后、跳过）每次只写入一条事件，且只创建一条后续调度。恢复睡眠或重启时最多补发一次过期提醒。
- 平台能力（托盘、勿扰、通知、全屏检测）不可用时要优雅降级，不能影响提醒与笔记的核心功能。
- 所有交互必须可键盘操作，有可见焦点和语义标签；支持浅色和深色系统主题。

## 本地检查

修改前端后运行 `npm run lint`、`npm run build` 和相关 `npm test`。修改 Rust 后运行 `cargo fmt --check` 与 `cargo check --manifest-path src-tauri/Cargo.toml`。如环境中缺少 Node.js 或平台依赖，请在交付说明中明确标注未能执行的检查。

## Git 提交

- 每次修改软件功能、行为、配置或用户界面时，必须在同一任务中同步更新 `app-spec.md` 的相关说明、平台约束和验收标准。
- 每次完成用户要求的代码或文档修改并通过适用检查后，自动创建一次本地 Git 提交，无需再次询问。
- 提交前只暂存本次任务产生的文件，不得夹带用户已有或其他任务留下的未提交修改。
- 提交信息应简洁说明本次改动；如果检查未通过或修改尚未完成，不得提交，并在交付说明中说明原因。

## 安全与隐私

- 不新增遥测、远程资源、账户、云同步或第三方分析。
- 不把用户笔记、提醒记录或浏览状态写进日志。
- 清除本地数据必须有二次确认；导出数据前应让用户明确选择位置。
- 原生消息通道必须验证来源、令牌与消息 schema，拒绝未授权扩展和未知字段。
